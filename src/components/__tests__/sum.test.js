import { sum } from "../sum";

test("Sum function should caculate the sum of two numbers", () => {
  const result = sum(3, 4);

  //Assertion
  expect(result).toBe(7);
});
/**
 * test("string",(callback function hai ye)=>{
 * })
 */
/**
 * test ya it dono same
 */
